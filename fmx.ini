<?php
// set the local timezone
ini_set('date.timezone','America/New_York');

// upload dialog in small popup window
// remove (or comment out) for modal overlay upload dialog
$fmx_upload_winpop = 1;

// show all errors
error_reporting(-1);

// instance is part of a Joomla! extension (experimental)
$fmxInJoomla = false;

// whether or not to use the command line interface
$fmx_ui_cli = 1;
